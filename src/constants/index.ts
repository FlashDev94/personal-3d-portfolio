/** @deprecated Prefer usePortfolio() — static defaults kept for reference */
import { defaultPortfolioData } from "./defaults";

export const navLinks = defaultPortfolioData.navLinks;
export const services = defaultPortfolioData.services;
export const technologies = defaultPortfolioData.technologies;
export const experiences = defaultPortfolioData.experiences;
export const testimonials = defaultPortfolioData.testimonials;
export const projects = defaultPortfolioData.projects;
